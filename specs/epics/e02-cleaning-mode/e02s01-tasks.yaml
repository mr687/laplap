story_id: e02s01
title: "Fullscreen black overlay + hidden cursor + CMDx6 exit"
status: failing
bcps: 5
tasks:
  - id: 1
    description: "OverlayController: one black NSWindow per NSScreen, screenSaver window level, ignoresMouseEvents, created on screen change notifications"
    verify: "swift test && swift build"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["overlay", "multi-display"]
  - id: 2
    description: "Centered instruction label ('CLEANING MODE — press CMD 6 times to exit') on each overlay; text styling plain white on black"
    verify: "swift test"
    risk: P2
    status: failing
    allure:
      severity: normal
      categories: ["overlay", "ui"]
  - id: 3
    description: "Cursor hide on arm (CGDisplayHideCursor), show on exit (CGDisplayShowCursor) — both in clean mode only"
    verify: "swift test"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["overlay"]
  - id: 4
    description: "Wire clean mode end-to-end: input lock + overlay + cursor; CMDx6 unlock restores cursor, closes overlays, exits 0"
    verify: "swift test && swift build"
    risk: P0
    status: failing
    allure:
      severity: critical
      categories: ["integration"]
