import Foundation

/// Single source of truth for the laplap version string.
/// Keep in sync with the git tag at release time (release checklist).
enum LaplapVersion {
    static let current = "0.1.0"
}
